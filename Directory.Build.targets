<Project>
  <!-- Override _NotifySpotlight to prevent mdimport from running (hangs on shared VMs) -->
  <Target Name="_NotifySpotlight">
    <Message Text="Skipping Spotlight indexing (disabled via Directory.Build.targets)" Importance="high" />
  </Target>
</Project>
